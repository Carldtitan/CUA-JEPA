diff --git a/websites/github_mock/src/components/Layout.jsx b/websites/github_mock/src/components/Layout.jsx
--- a/websites/github_mock/src/components/Layout.jsx
+++ b/websites/github_mock/src/components/Layout.jsx
@@ -1,7 +1,7 @@
 
     import React, { useState, useRef, useEffect } from 'react';
     import { Outlet, Link, useNavigate } from 'react-router-dom';
-    import { XitHub, Bell, Plus, Search, X, MessageSquare, GitPullRequest, AtSign, AlertCircle, CheckCircle } from 'lucide-react';
+    import { Github, Bell, Plus, Search, X, MessageSquare, GitPullRequest, AtSign, AlertCircle, CheckCircle } from 'lucide-react';
     import { useStore } from '../lib/store';
     import KeyboardNav from './KeyboardNav';
     import CommandPalette from './CommandPalette';
@@ -105,7 +105,7 @@
           <div className="max-w-[1280px] mx-auto px-4 h-16 flex items-center gap-4">
             {/* Logo */}
             <Link to="/" className="flex-shrink-0">
-                <XitHub size={32} fill="white" />
+                <Github size={32} fill="white" />
             </Link>
 
             {/* Search */}
@@ -155,7 +155,7 @@
                               {searchResults.repos.map(repo => (
                                 <Link key={repo.id} to={`/${repo.owner}/${repo.name}`} className="flex items-center gap-2 px-3 py-2 hover:bg-xithub-bg rounded">
-                                  <XitHub size={16} className="text-xithub-muted" />
+                                  <Github size={16} className="text-xithub-muted" />
                                   <div>
                                     <div className="font-medium text-sm">{repo.owner}/{repo.name}</div>
                                     <div className="text-xs text-xithub-muted truncate">{repo.description}</div>
